# This fixture is pinned by sha256 in BOTH repos. EOL conversion on checkout
# breaks that pin on Windows while changing nothing any test asserts.
src/utils/__tests__/fixtures/premium-voice-cases.json -text
src/utils/__tests__/fixtures/catalogue_key_cases.json -text
