# Prod QA smoke — app.sketchcast.app — 2026-07-12

**Run by:** qa-frontend agent method (autonomous, unauthenticated pass).
**Target:** `https://app.sketchcast.app` (portal) + `https://sketchcast.app` (landing).
**Session:** none — this pass covers everything reachable without typing a secret. Scenarios
needing a logged-in session are listed under *Blocked (needs human session)*.

## Result: PASS (autonomous scope) — 0 failures, 2 items to confirm

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | Root `/` unauthenticated | ✅ PASS | Serves the sign-in form; 0 console errors; all assets `200`. |
| 2 | Protected routes gate anon → `/login` | ✅ PASS | `/dashboard`, `/dashboard/children`, `/dashboard/analytics`, `/dashboard/school`, `/onboarding`, `/console`, `/console/users` all `302 → /login`. |
| 3 | **New onboarding gate deployed** | ✅ PASS | `/onboarding` (anon) → `/login`; route is live, not 404. |
| 4 | **`FEATURE_ONBOARDING` is ON in prod** | ✅ PASS | `POST /api/onboarding` (anon) → `401 "Not signed in."` If the flag were off the code returns `404` first — 401 proves the flag passed and auth is enforced. |
| 5 | API auth contracts | ✅ PASS | `POST /api/assistant`, `/api/school-finish`, `/api/students` (anon) → `401 "Not signed in."` |
| 6 | Public pages render | ✅ PASS | `/signup`, `/login/forgot`, `/schoolsignup` → `200`, no redirect. |
| 7 | Signup role picker | ✅ PASS | Renders name/email/password + **teacher/student/parent** toggle + Google → `NEXT_PUBLIC_FEATURE_PARENT_PORTAL` live; feeds the onboarding seed. |
| 8 | `GET /api/onboarding` | ✅ PASS | `405` (POST-only) — route present, method-guarded. |
| 9 | 404 handling | ✅ PASS | Unknown path → real `404`. |
| 10 | Landing + pricing | ✅ PASS (load) | `sketchcast.app/pricing` `200`, tiers render ($0 / $24 / $49 / $10 founding); CTAs → `app.sketchcast.app/signup`. |

## Items to confirm (not failures)

- **Pricing CTAs route to `/signup`, not to Lemon Squeezy checkout.** The live `/pricing`
  buy buttons ("Start free trial") all point at `app.sketchcast.app/signup`; there are **zero**
  `lemonsqueezy.com` links on the page. This is consistent with a trial-first model (upgrade
  in-app), but memory recorded "real LS checkout links in `pricing.config.js`." **Confirm this
  is the intended path** (freemium/trial-first) vs. a lost checkout link.
- **Screenshots timed out** on the pricing renderer (heavy fonts/animation). Structure was
  verified via DOM/JS instead. Cosmetic-only; no functional impact observed.

## Blocked — needs a human-authenticated session (the high-value next pass)

These require logging in (I can't type passwords), so they're the top candidates for the
session-handoff run. Highest value first:

1. **Onboarding end-to-end** — re-register `junaidisara8@gmail.com` (now freed), confirm the
   blocking `/onboarding` step appears, that "Continue" stays disabled until required fields
   are filled, and that submit lands on `/dashboard` (teacher) or `/dashboard/children`
   (parent) and never shows the gate again. *This is the freshest feature — test it first.*
2. **Lesson generation** — upload a chapter, generate a lesson, confirm it completes in the
   improved time and plays.
3. **AI Teaching Assistant** — launcher present on every page/role, grounded answer on an
   in-scope book, math tool.
4. **Onboarding tour** — coach-marks fire per role; replay button works.
5. **Report-an-issue** — submit and confirm the escalation email now reaches
   `muqtadar.quraishi@sketchcast.app` (RESEND key was added to the worker).
6. Role nav correctness (teacher/parent/student/school_admin/coordinator), console admin
   surfaces, parent portal, analytics.

## Authenticated pass (demo accounts, password-free sessions) — PASS

Sessions were established **without typing passwords**: a service-role magic-link token →
`/auth/confirm` (`verifyOtp`), for `demo.*` accounts only. No destructive actions were fired.

| # | Check | Role | Verdict | Evidence |
|---|-------|------|---------|----------|
| A1 | Session + dashboard | teacher (demo.teacher2) | ✅ PASS | Lands on `/dashboard` "Your library"; nav = **Library + My Analytics**; header "· teacher"; 0 console errors. |
| A2 | Assistant launcher everywhere | teacher/parent/student | ✅ PASS | "Open the AI Teaching Assistant" present for all three roles. |
| A3 | Feedback/Report buttons | teacher | ✅ PASS | Give feedback + Report a problem + Assistant all present & aligned (students see Report + Assistant only — feedback is adult-only). |
| A4 | Onboarding tour auto-start | teacher/student | ✅ PASS | Coach-marks fire; `POST /api/tour/event → 200` ×6. |
| A5 | **Onboarding gate E2E** | teacher (demo.teacher6) | ✅ PASS | Nulled `onboarded_at` → `/dashboard` redirects to `/onboarding`; name prefilled; **Continue blocked** until affiliation+grade+subject set (hint shown); enabled when complete; `POST /api/onboarding → 200`; lands `/dashboard`; **gate no longer fires** on re-fetch. |
| A6 | Assistant — grounded answer | student (demo.t1.s1) | ✅ PASS | "What is Unit 1 about?" → grounded summary, source-cited "📖 from your Chapter 0 — Unit 1: Be a designer"; closed-book framing intact. |
| A7 | Assistant — no-books handling | teacher (empty library) | ✅ PASS | Graceful "I don't see a book in your study list yet" instead of erroring. |
| A8 | Parent portal | parent (demo.parent1) | ✅ PASS | Nav = **Library / My Analytics / My Children / Test Papers** (full-author); `/dashboard/children` shows linked children (Demo Student 1-1/1-2), their assigned schoolwork + statuses, Coach recap, per-child **Reset password**. |
| A9 | Student experience + RLS | student (demo.t1.s1) | ✅ PASS | "My lessons" shows ONLY assigned content ("Unit 1: Be a designer · ✓ Completed · Watch · Deck"); no adult nav tabs; signed artifact URLs. |

**Notes from the authenticated pass:**
- Minor copy nit — the assistant's no-books message tells a **teacher** to "ask your teacher
  or parent to add one" (student-oriented wording shown regardless of role).
- The math tool could not be exercised in-context: the assistant is book-gated, and no demo
  account has a **maths** book in scope (the one seeded book is a design unit). Add a maths
  book to a demo teacher to smoke the SymPy path via the UI. (The definite-integral contract
  fix is already covered by app + worker unit tests.)
- Full lesson **generation** was not run end-to-end (needs a fresh PDF upload + ~10 min); a
  previously-generated lesson ("Unit 1: Be a designer") plays and downloads correctly, so the
  render→store→serve path is confirmed live.
- QA side effect: `demo.teacher6`'s profile now holds QA-entered values (Independent / Grades
  7–9 / Science) and a fresh `onboarded_at`; it is in a valid onboarded state.

## How this was run

Unauthenticated routing/guard/API-contract probes via same-origin `fetch()` and
`read_page`; console + network inspected (clean). Authenticated flows ran inside password-free
demo sessions minted via the service role (magic-link → `/auth/confirm`), `demo.*` accounts
only. No secrets typed, no accounts created, no destructive/irreversible actions fired — per
the QA agent safety boundary.
